name: Jira PR Comment

on:
  pull_request:
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch: {}

jobs:
  notify-jira:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Detecta la clave JIRA (ABC-123) desde el título del PR; puedes ampliar a rama/commit si quieres
      - name: Extract JIRA issue key
        id: jirakey
        shell: bash
        run: |
          KEY=""
          TITLE="${{ github.event.pull_request.title }}"
          if [[ "$TITLE" =~ ([A-Z]+-[0-9]+) ]]; then KEY="${BASH_REMATCH[1]}"; fi
          echo "Detected JIRA KEY: $KEY"
          echo "key=$KEY" >> "$GITHUB_OUTPUT"
          if [ -z "$KEY" ]; then
            echo "WARNING: No JIRA key detected in PR title (e.g. GG-13: ...)" >&2
          fi

      - name: Jira login
        if: steps.jirakey.outputs.key != ''
        uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: https://gislaine-guedes.atlassian.net
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}

      - name: Comment on issue
        if: steps.jirakey.outputs.key != ''
        uses: atlassian/gajira-comment@v3
        with:
          issue: ${{ steps.jirakey.outputs.key }}
          comment: |
            🚀 PR creado: ${{ github.event.pull_request.html_url }}
            📦 Rama: `${{ github.head_ref }}`
            👤 Autor: ${{ github.actor }}

      # Mover a "En curso" (estado activo) al abrir el PR
      - name: Move to Work In Progress
        if: steps.jirakey.outputs.key != '' && github.event.action == 'opened'
        uses: atlassian/gajira-transition@v3
        with:
          issue: ${{ steps.jirakey.outputs.key }}
          transition: "Iniciar Progreso"   # ← este es el nombre válido según JIRA

